service:
  type: ClusterIP

nodeSelector:
  node-role.kubernetes.io/ingress: ""

ports:
  web:
    hostPort: 80
  websecure:
    hostPort: 443

ingressRoute:
  dashboard:
    enabled: true

additionalArguments:
  - "--api.insecure=true"
  - "--api.dashboard=true"
